version: '3.8'

services:


  test:
    build:
      context: ../..
      args:
        HOME: ${HOMEDIR}
        IMAGE: ${BASE_IMAGE}
      dockerfile: test/docker/Dockerfile
    volumes:
      - ../../custom:${HOMEDIR}
    command: pytest --verbose
