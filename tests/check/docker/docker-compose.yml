version: '3.8'

x-common:
   &default
    image: ${PACKAGE}-check
    build:
      context: ../../..
      args:
        PKG: ${PACKAGE}
        HOME: ${HOMEDIR}
        IMAGE: ${BASE_IMAGE}
      dockerfile: tests/check/docker/Dockerfile

services:

  flake:
    <<: *default
    command:
      - /bin/sh
      - -c
      - |
          # stop the build if there are Python syntax errors or undefined names
          flake8 ${PACKAGE} --count --select=E9,F63,F7,F82 --show-source --statistics
          # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
          flake8 ${PACKAGE} --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

  bandit:
    <<: *default
    command: bandit -r ${PACKAGE}
